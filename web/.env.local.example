# Local development (default — no change needed)
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Production — set this in Vercel dashboard to your Railway URL, e.g.:
# NEXT_PUBLIC_API_URL=https://your-app.railway.app
